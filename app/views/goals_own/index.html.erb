<div class="view">
  <div class="full-bg-img flex-center">
    <div class="container ml-auto mb-5">
         <h5 class="display-4 ml-auto" style="color: dimgray">My Goals</h5>

<p id="notice"><%= notice %></p>
<table class="table table-hover">
  <thead>
  <tr>
    <th scope="col">Title</th>
    <th scope="col">Synopsis</th>
    <th scope="col">Duedate</th>
    <th scope="col">Funds goal</th>
    <th scope="col">Funds current</th>
    <th scope="col">Player</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% if @goals != nil %>
    <% @goals.each do |goal| %>
      <tr class="table-dark">
        <td scope="row"><%= goal.title %></td>
        <td><%= goal.synopsis %></td>
        <td><%= goal.duedate %></td>
        <td><%= goal.funds_goal %></td>
        <td><%= goal.funds_current %></td>
        <td><%= goal.player %></td>
        <td><%= link_to 'Show', goal %></td>
        <td><%= link_to 'Edit', edit_goal_path(goal) %></td>
        <td><%= link_to 'Destroy', goal, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Goal', new_goal_path %>

</div>
</div>
</div>