<p id="notice"><%= notice %></p>

<h1>Goals</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Campaigng</th>
      <th>Duedate</th>
      <th>Funds goal</th>
      <th>Funds current</th>
      <th>Player</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @goals.each do |goal| %>
      <tr>
        <td><%= goal.title %></td>
        <td><%= goal.campaigng %></td>
        <td><%= goal.duedate %></td>
        <td><%= goal.funds_goal %></td>
        <td><%= goal.funds_current %></td>
        <td><%= goal.player %></td>
        <td><%= link_to 'Show', goal %></td>
        <td><%= link_to 'Edit', edit_goal_path(goal) %></td>
        <td><%= link_to 'Destroy', goal, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<% if @user !=  nil %>
  <% if @player != nil %>
    <%= link_to 'New Goal', new_goal_path %>
  <% else %>
    <%= link_to 'Want to create a goal? Click here to start with the process', new_player_path %>
  <% end %>
<% else %>
  <%= link_to 'Log in to start creating your goals!', new_user_session_url %>
<% end %>